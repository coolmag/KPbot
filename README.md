# 🤖 AI Client Pilot — Генератор коммерческих предложений

Telegram-бот, который создаёт профессиональные КП за 30 секунд.

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Скачивание шрифта (кириллица)
Перед запуском скачайте шрифт с поддержкой кириллицы:

**Вариант A — автоматически:**
```bash
python download_font.py
```

**Вариант B — вручную:**
1. Скачайте [DejaVuSans.ttf](https://github.com/dejavu-fonts/dejavu-fonts/raw/master/fonts/DejaVuSans.ttf)
2. Положите файл в корневую папку проекта

### 3. Настройка окружения
Создайте файл `.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
OPENROUTER_API_KEY=your_openrouter_key_here
```

### 4. Запуск
```bash
python bot.py
```

## 📋 Команды бота
- `/start` — начать создание КП
- `/cancel` — отменить

## 🛠 Технический стек
- Python 3.10+
- python-telegram-bot 20.x
- OpenAI API / OpenRouter
- ReportLab (PDF)

## 📦 Деплой на Railway
1. Загрузите код на GitHub
2. Создайте проект на [Railway](https://railway.app)
3. Подключите репозиторий
4. Добавьте переменные окружения
5. В Procfile: `worker: python bot.py`

## 📄 Структура проекта
```
├── bot.py              # Основной бот с FSM
├── ai_service.py       # Интеграция с AI
├── pdf_generator.py    # Генерация PDF
├── download_font.py    # Скрипт для скачивания шрифта
├── DejaVuSans.ttf      # Шрифт (добавить вручную)
├── requirements.txt    # Зависимости
├── Procfile            # Конфиг для Railway
└── .env.example        # Пример переменных
```
