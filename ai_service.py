from google import genai
import os
import logging

logger = logging.getLogger(__name__)

# Попытка инициализировать клиент.
# Это может вызвать ошибку, если API-ключ не установлен, но мы обработаем это позже.
try:
    client = genai.Client(api_key=os.getenv("GOOGLE_API_KEY"))
except Exception as e:
    client = None
    logger.warning(f"Клиент Gemini не был инициализирован при загрузке модуля: {e}")

# Список моделей для попытки подключения в порядке приоритета.
PREFERRED_MODELS = [
    "gemini-pro", 
    "gemini-1.0-pro", # Часто используемый стабильный псевдоним
    # Можно добавить другие модели в будущем, например, "gemini-2.5-pro"
]

def get_proposal_text(prompt: str) -> str:
    """
    Генерирует КП, используя отказоустойчивую цепочку моделей Gemini.
    """
    if not client:
        logger.error("❌ Клиент Gemini не был сконфигурирован. Проверьте GOOGLE_API_KEY.")
        return (
            "Коммерческое предложение (Черновик)\n\n"
            "Сервис AI не сконфигурирован (отсутствует API-ключ)."
        )

    last_error = None
    
    full_prompt = (
        f"Ты — профессиональный бизнес-ассистент. Составь коммерческое предложение "
        f"на основе данных: {prompt}. "
        f"Структура: Приветствие, Понимание задачи, Предлагаемое решение, Сроки и стоимость (примерные), Призыв к действию. "
        f"Форматирование: Не используй Markdown (жирный, курсив, заголовки #), "
        f"пиши простым текстом, разделяя смысловые блоки пустыми строками."
    )

    for model_name in PREFERRED_MODELS:
        try:
            logger.info(f"🤖 Пробую модель: {model_name}")
            model = client.generative_model(model_name)
            response = model.generate_content(full_prompt)
            
            # getattr для безопасной проверки наличия атрибута .text
            text = getattr(response, "text", None)
            if text:
                logger.info(f"✅ Модель {model_name} успешно сгенерировала ответ.")
                return text.strip()
            else:
                logger.warning(f"⚠️ Модель {model_name} вернула пустой ответ.")
                last_error = ValueError(f"Empty response from model {model_name}")
                continue

        except Exception as e:
            last_error = e
            logger.warning(f"⚠️ Модель {model_name} недоступна. Ошибка: {e}")
            continue

    # Этот блок выполнится, только если ни одна модель не сработала
    logger.error("❌ Ни одна из предпочтительных моделей Gemini не сработала.", exc_info=last_error)
    return (
        "Коммерческое предложение (Черновик)\n\n"
        "К сожалению, сервис генерации временно недоступен. "
        "Мы получили ваши вводные данные и свяжемся с вами лично."
    )
